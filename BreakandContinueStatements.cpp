#include <iostream>
using namespace std;
int main() {
  
  /* break statement 
  int sum = 0;
  cout << "Enter values for x" << endl;
  double x;
  for(int k=1; k <=3; ++k)
  {
    cin >> x; 
    if(x > 10.0)
      break;
    sum += x; 

  }
  cout << "Sum = " << sum << endl; 

  */

  /* continue statement 
  int sum = 0;
  cout << "Enter values for x" << endl;
  double x;
  for(int k=1; k <=3; ++k)
  {
    cin >> x; 
    if(x > 10.0) //the sum of all values enter will print if this                                                    condition is true 
      continue; //otherwise if a value is greater than 10.0 the continue statement cause the control to skip the rest of the statements in that iteration of the loop and continue with the next iteration of the loop. 
    sum += x; 

  }
  cout << "Sum = " << sum << endl; 
 */ 

}